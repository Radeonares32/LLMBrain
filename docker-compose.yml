version: '3.8'

services:
  llmbrain-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    command: llmbrain serve --host 0.0.0.0 --port 8000
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
